import React from "react";

const Header = () => {
  return (
    <nav className="navbar navbar-light bg-light">
      <div className="container-fluid">
        <h2 className="fw-bolder text-primary">Play!</h2>
      </div>
    </nav>
  );
};

export default Header;
